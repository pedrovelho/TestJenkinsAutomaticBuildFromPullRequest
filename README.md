# TestJenkinsAutomaticBuildFromPullRequest
Project to test the integration with jenkins. Aim at automatically detect a newly job exists on the same PR avoiding  redundant useless jobs to live on the queue.
